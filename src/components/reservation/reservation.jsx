import React from 'react'
import Style from '../../styles/reservation.module.scss'

function Reservation(props) {
    return (
        <section className={Style.reservationMainWrapper}>
            <h2>Reservationer</h2>
        </section>
    )
}

export default Reservation